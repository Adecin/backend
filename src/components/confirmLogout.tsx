import { Dialog } from "@mui/material";
import CustomButton from "./customButton";
import PopCloseIcon from "./popcloseIcons";

const LogoutPop = (props: any) => {
  const { onClose } = props;
  return <div></div>;
};

export default LogoutPop;
